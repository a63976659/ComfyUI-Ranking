# AGENTS.md — ComfyUI-Ranking Agent 导航

> 本文件是 AI Agent 理解本项目的第一入口。修改任何代码前，请先阅读本文件与对应的技术文档章节；修改完成后，按文末"修改前后检查规范"核查关联功能。

---

## 1. 项目概述

ComfyUI-Ranking（RanKing）是一个深度集成于 ComfyUI 的内容社区插件，支持工具发布、应用分享、资源推荐、任务委托，打造 ComfyUI 生态的价值闭环（创作变现、榜单排序、赞赏分享、开放 API）。

三层架构：

| 层 | 位置 | 技术 | 职责 |
|----|------|------|------|
| ① 本地插件层 | 仓库根目录 | Python + aiohttp（复用 ComfyUI PromptServer 路由，无独立端口） | 插件安装 / 工作流下载 / 媒体缓存代理 / 插件自更新 |
| ② 前端层 | `前端页面/` | 原生 JavaScript (ES Module)，无构建工具，动态 `import()` 按需加载 | 侧边栏形态的榜单 UI、发布、钱包、社交 |
| ③ 云端层 | `云端Space代码/` | Python FastAPI + Pydantic，HF Spaces Docker 部署 | 账号、内容、钱包、任务、私信、开放 API |

关键契约：

- 本地路由统一挂载于 `/community_hub/*`（仅本机可访问）
- 前端通过 HTTPS REST 访问云端 `API.BASE_URL`（配置于 `前端页面/core/全局配置.js`）
- 云端双数据层：资金类走 SQL（`database_sql.py`），业务类走 JSON 文件（`数据库连接.py` + HF Dataset 同步）
- 前端所有常量集中于 `前端页面/core/全局配置.js`，业务组件禁止硬编码

详细架构见 [技术文档/01_项目概述与总体架构.md](./技术文档/01_项目概述与总体架构.md)。

---

## 2. 核心路径（高影响区域）

以下路径是项目核心边界，任何改动都属于高影响变更，必须格外谨慎并做关联核查。

### 2.1 本地插件层（根目录）

| 文件 | 职责 |
|------|------|
| `__init__.py` | ComfyUI 插件入口：路由注册、启动时自更新 |
| `api_tool.py` | 工具安装引擎（安装路由：git clone / 私有库 / ZIP 流式安装） |
| `api_app.py` | 工作流下载引擎（本地缓存 + 云端回退） |
| `api_cache.py` | 图片/视频缓存代理（Range 流式、下载锁） |

### 2.2 前端核心（`前端页面/core/`，11 个文件）

| 文件 | 职责 |
|------|------|
| `侧边栏入口注册.js` | ComfyUI 扩展注册（**极少修改，不可随意改动**） |
| `侧边栏主程序.js` | DOM 构建、视图路由、Tab/排序/搜索 |
| `侧边栏数据引擎.js` | 榜单数据加载与分页 |
| `全局配置.js` | 全局配置中心（API 地址、缓存 TTL、管理员账号等） |
| `状态管理.js` | 全局状态（用户会话 / 视图状态 / 内存缓存） |
| `类型定义.js` | JSDoc 类型定义 |
| `网络请求API.js` | 网络层统一导出入口 |
| `网络请求_基础设施.js` | fetch 封装、重试、去重、取消管理 |
| `网络请求_图片代理.js` | proxyImages/unproxyImages |
| `网络请求_缓存管理.js` | 多级缓存底层 |
| `网络请求_业务API.js` | api 对象：40+ 业务方法 |

### 2.3 前端认证（`前端页面/auth/`，6 个文件）

认证表单组件 5 个：`用户注册登录组件.js`、`登录表单组件.js`、`注册表单组件.js`、`重置密码表单组件.js`、`找回账号表单组件.js`；另有数据文件 `国家地区数据.js`。涉及登录/注册/密码/账号找回的任何改动都属高影响变更。

### 2.4 云端层核心（`云端Space代码/`）

- `app.py`：FastAPI 入口（路由挂载、全局异常、健康检查、上传）
- `安全认证.py`：bcrypt 密码哈希、JWT 签发验证、管理员判定
- `router_*.py`：业务路由（用户×3 / 内容 / 评论 / 私信 / 钱包 / 代理 / 帖子 / 任务 / 开放 API / 插件管理）
- `数据库连接.py` / `database_sql.py`：JSON 数据层与 SQL 数据层

---

## 3. 技术文档索引（13 篇）

所有技术文档位于 `技术文档/` 目录，全部基于当前代码实现编写。**修改对应功能前先阅读相关章节**，关键契约（接口路径、传参方式、存储字段名）须在代码中核实后再使用。

| 文档 | 内容 |
|------|------|
| [01_项目概述与总体架构.md](./技术文档/01_项目概述与总体架构.md) | 三层架构、数据流、目录结构、技术栈与部署形态 |
| [02_本地插件引擎.md](./技术文档/02_本地插件引擎.md) | `/community_hub/*` 本地路由、下载安装、缓存代理、插件自更新、三发行版兼容 |
| [03_前端核心架构.md](./技术文档/03_前端核心架构.md) | 侧边栏入口、全局配置、状态管理、模块组织 |
| [04_网络请求与缓存系统.md](./技术文档/04_网络请求与缓存系统.md) | 请求核心、多级缓存、图片代理、敏感数据脱敏 |
| [05_认证与安全体系.md](./技术文档/05_认证与安全体系.md) | 密码/Token、管理员与云端控制台、验证码与账号找回、限流、图片审核 |
| [06_钱包与交易系统.md](./技术文档/06_钱包与交易系统.md) | 余额模型、充值提现、阶梯手续费、哈希链流水、购买与退款 |
| [07_市场与资源系统.md](./技术文档/07_市场与资源系统.md) | 榜单与排序框架、发布、安装引擎、版本检测与自更新 |
| [08_任务榜与仲裁系统.md](./技术文档/08_任务榜与仲裁系统.md) | 任务状态机、资金托管、申诉与管理员仲裁 |
| [09_社交互动系统.md](./技术文档/09_社交互动系统.md) | 点赞收藏、评论、讨论区、私信、通知中心、提示词市场 |
| [10_下载量与浏览量统计.md](./技术文档/10_下载量与浏览量统计.md) | 下载量去重计数、浏览量双计数、每日重置任务 |
| [11_数据库连接与同步配置.md](./技术文档/11_数据库连接与同步配置.md) | JSON 数据层、HF Dataset 批量同步、环境变量配置 |
| [12_国际化与用户体验增强.md](./技术文档/12_国际化与用户体验增强.md) | 多语言系统、语言切换联动、翻译插件防护、体验增强工具集 |
| [13_开放API与第三方插件接入.md](./技术文档/13_开放API与第三方插件接入.md) | 开放 API 接口规范、Plugin Key 鉴权、插件管理 API 与接入示例 |

---

## 4. 修改前后检查规范

### 4.1 修改前：梳理功能、划定范围

1. **阅读对应技术文档章节**，理解待改功能的现状与关联关系。
2. **严格划定修改范围**：只修改本次任务明确涉及的部分，未提及的内容禁止修改；不要自作主张添加额外功能、UI 元素或逻辑变更。
3. **核实关键契约**：接口路径、传参方式、存储字段名等须在代码中逐一核实，禁止凭记忆推断。
4. **识别核心边界**：若涉及第 2 节列出的核心路径，属高影响变更，需先评估对调用方的影响（如 `网络请求API.js` 导出面、`全局配置.js` 配置键、`/community_hub/*` 路由契约）。

### 4.2 修改后：核查关联功能

对照以下 8 个作用域逐一核查本次改动是否产生连带影响，无影响也要明确确认：

| # | 作用域 | 核查要点 |
|---|--------|----------|
| 1 | 前端核心（`前端页面/core/`） | 入口注册、全局配置键、状态结构、模块导出面是否被破坏；动态导入时序是否正确 |
| 2 | 网络请求与缓存 | 请求基础设施、多级缓存 TTL 与失效机制、图片代理（`proxyImages` 必须保持动态 import 时序） |
| 3 | 本地插件引擎 | `/community_hub/*` 路由契约、安装/下载/缓存代理行为、三发行版兼容 |
| 4 | 云端 API 与数据层 | 路由契约、JSON/SQL 双数据层字段、HF Dataset 同步、并发与原子操作 |
| 5 | 认证与安全 | JWT 签发验证、管理员判定、限流、图片审核、敏感数据脱敏 |
| 6 | 钱包与交易 | 余额一致性、手续费阶梯、哈希链流水、支付回调 |
| 7 | 国际化与 UI 一致性 | 所有用户可见文本走 i18n（禁止硬编码、禁止 fallback 兜底模式）、返回按钮等统一 UI 规范 |
| 8 | 技术文档与 README 同步 | 代码变更后在同一任务内同步相关章节（架构图、目录树、组件描述），并检查 README 更新记录是否需补充条目 |

### 4.3 技术文档维护规则（摘自 `.qoder/rules/技术文档规则.md`）

1. **结构稳定**：文档章节编号与文件名保持稳定，不随意更名或调整编号，避免破坏目录索引与交叉引用。
2. **同步更新**：代码新增、变更、删除或迁移功能时，同一任务内同步相关章节；完成后全库扫描过时措辞，确认无遗留描述。
3. **编写依据**：文档必须基于当前代码实现编写，关键契约逐一在代码中核实后再写入。
4. **关联检查**：同步技术文档时，同时检查项目 README 更新记录是否需要补充对应条目。
